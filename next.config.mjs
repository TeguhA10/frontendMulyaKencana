/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images2.alphacoders.com', 'static.vecteezy.com'], // Tambahkan domain di sini
    },
};

export default nextConfig;
